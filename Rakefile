require 'rake'
require 'panamax_template_validator'

task :default do
  # `git remote add source https://github.com/CenturyLinkLabs/panamax-contest-templates.git`
  # `git fetch source`

  changed_files = `git diff --stat source/master`


  # PanamaxTemplateValidator.validate
end
